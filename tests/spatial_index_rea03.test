[test_case]
test = perfrunner.tests.view.InitialAndIncrementalSpatialTest
title = 1 bucket x 6M x 3d, non-DGM, 1 x 1 spatial view, no mutations
summary = Spatial indexing test, 4 nodes, 1 bucket x 6M x 3d, non-DGM, 4 x 4 spatial views (rea03 dataset)
larger_is_better = false

[cluster]
mem_quota = 40960
initial_nodes = 4
num_buckets = 1

[compaction]
db_percentage = 100
view_percentage = 100

[load]
items = 11958999
items = 6000000
size = 1024
workers = 20
# XXX vmx 2015-05-08: Perhaps move this `dimensions` and `filename` to the spatial section
doc_gen = spatial
dimensionality = 3
data = /tmp/spatial/data/rea03

[spatial]
indexes =
    main::{"spatial":{"geom":"function (doc) {\n  if (doc.a !== undefined && doc.b !== undefined) {\n    emit([doc.a, doc.b], null);\n  }\n}"}}
disabled_updates = 1

[access]
creates = 100
ops = 5958999
items = 6000000
size = 1024
workers = 2
# XXX vmx 2015-05-08: this `spatial_workers` and `spatial_throughput` should be moved int the [spatial] section
spatial_workers = 0
#spatial_throughput =
doc_gen = spatial
dimensionality = 3
data = /tmp/spatial/data/rea03
